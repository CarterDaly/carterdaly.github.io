export function ContactSection() {
  // Minimal anchor to preserve in-page navigation; content now lives in the footer.
  return (
    <section id="contact" className="sr-only" aria-hidden="true">
      Contact
    </section>
  );
}
