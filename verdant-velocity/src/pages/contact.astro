---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionTitle from '../components/SectionTitle.astro';
import { siteConfig } from '../config/siteConfig';
---

<BaseLayout title={`Contact — ${siteConfig.name}`} description="Get in touch with Carter Daly.">
  <section class="px-6 pb-16 pt-10 sm:pt-12">
    <div class="mx-auto max-w-5xl space-y-8">
      <SectionTitle
        eyebrow="contact"
        title="Let’s collaborate"
        subtitle="Use the email link below or the quick form as a placeholder. No backend is wired."
      />
      <div class="grid gap-8 lg:grid-cols-[1.05fr,0.95fr]">
        <div class="space-y-4 rounded-2xl border border-white/10 bg-white/5 p-6 shadow-[0_20px_60px_rgba(0,0,0,0.35)]">
          <p class="text-muted">
            The form is non-functional by design—use the email/social links. When ready, swap in a real backend or keep mailto.
          </p>
          <form
            class="space-y-4"
            onsubmit={`event.preventDefault(); window.location.href='${siteConfig.links.email}';`}
          >
            <label class="block space-y-2">
              <span class="text-sm text-muted">Name</span>
              <input
                type="text"
                name="name"
                class="w-full rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-primary placeholder:text-muted focus:border-accent-primary focus:outline-none"
                placeholder="Your name"
              />
            </label>
            <label class="block space-y-2">
              <span class="text-sm text-muted">Email</span>
              <input
                type="email"
                name="email"
                class="w-full rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-primary placeholder:text-muted focus:border-accent-primary focus:outline-none"
                placeholder="you@example.com"
              />
            </label>
            <button
              type="submit"
              class="inline-flex items-center gap-2 rounded-full bg-accent-primary px-4 py-2 text-sm font-semibold text-surface transition hover:-translate-y-[1px] hover:shadow-glow focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-primary"
            >
              Email me
            </button>
          </form>
        </div>
        <div class="space-y-4 rounded-2xl border border-white/10 bg-gradient-to-br from-accent-primary/10 via-transparent to-accent-secondary/10 p-6 shadow-[0_20px_60px_rgba(0,0,0,0.35)]">
          <p class="text-sm uppercase tracking-[0.2em] text-muted">links</p>
          <div class="space-y-3 text-primary">
            <a class="flex items-center gap-2 transition hover:text-white" href={siteConfig.links.email}>Email</a>
            <a class="flex items-center gap-2 transition hover:text-white" href={siteConfig.links.linkedin}>LinkedIn</a>
            <a class="flex items-center gap-2 transition hover:text-white" href={siteConfig.links.github}>GitHub</a>
          </div>
          <p class="text-sm text-muted">
            More channels can be added later. The email link currently uses a TODO placeholder.
          </p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
