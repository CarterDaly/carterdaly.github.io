---
import { siteConfig } from '../config/siteConfig';

const year = new Date().getFullYear();
---

<footer class="border-t border-white/5 px-6 py-10">
  <div
    class="mx-auto flex max-w-6xl flex-col gap-4 text-sm text-muted sm:flex-row sm:items-center sm:justify-between"
  >
    <div class="flex flex-col gap-2">
      <div class="flex items-center gap-3 text-xs uppercase tracking-[0.14em]">
        <span class="text-primary">© {year}</span>
        <span>{siteConfig.name.toLowerCase()}</span>
        <span class="text-muted/70">{siteConfig.location}</span>
      </div>
      <p class="text-muted">
        Research-driven ML for fusion stability, race strategy, and resilient forecasting.
      </p>
    </div>
    <div class="flex flex-wrap items-center gap-4">
      <a
        href={siteConfig.links.github}
        class="transition hover:text-primary"
        target="_blank"
        rel="noreferrer"
      >
        GitHub
      </a>
      <a
        href={siteConfig.links.linkedin}
        class="transition hover:text-primary"
        target="_blank"
        rel="noreferrer"
      >
        LinkedIn
      </a>
      <a
        href={siteConfig.links.email}
        class="rounded-full bg-accent-warm px-4 py-2 text-sm font-medium text-black transition hover:brightness-110"
      >
        {siteConfig.contactCtaLabel}
      </a>
    </div>
  </div>
</footer>
